// Definitions of model-selected risks. No source patterns or local verdicts.
export const guidance: Record<string, [consequence: string, nextCheck: string, missing: string]> = {
  'cuda-indexing': ['Invalid indexing can address the wrong element or fail compilation.', 'Check index types and the flattened address calculation.', 'Index declarations and pointer types are needed.'],
  'cuda-bounds': ['Edge-tile threads can access memory outside the allocation.', 'Check load and store guards against the actual dimensions.', 'Allocation dimensions and launch shape are needed.'],
  'cuda-barriers': ['A barrier reached by only some required threads can stall or invalidate synchronization.', 'Check participation at matching block barriers.', 'Enclosing branches and the block launch are needed.'],
  'cuda-shared-lifetime': ['A shared tile may be overwritten while another thread still reads it.', 'Check synchronization between the final read and next tile load.', 'The producer, consumer and enclosing tile loop are needed.'],
  'cuda-cooperative-loads': ['Duplicate loads waste work and can race on a shared location.', 'Check which thread owns each shared-memory load.', 'Thread-to-tile mapping and launch shape are needed.'],
  'cuda-coalescing': ['Scattered warp addresses can require extra memory transactions.', 'Check adjacent lanes’ addresses and measure transaction efficiency.', 'Layout, strides and lane mapping are needed.'],
  'cuda-accumulator': ['Repeated global updates can add traffic or depend on uninitialized output.', 'Check initialization and whether a local accumulator can feed one final store.', 'Output initialization and the full accumulation path are needed.'],
  'cuda-thread-work': ['A narrow thread branch serializes cooperative work while other threads wait.', 'Check whether lanes can share the reduction before the final store.', 'The enclosing thread branch and cooperative-work contract are needed.'],
  'cuda-reuse': ['Repeatedly loading reusable operands can make memory traffic dominate.', 'Check operand reuse across neighboring outputs.', 'Operand layout and repeated computation are needed.'],
  'cuda-resources': ['Large live state can limit resident blocks or spill registers.', 'Inspect compiled resources and achieved occupancy for the target launch.', 'GPU, launch shape and compiled resource usage are needed.'],
  'cuda-overlap': ['A load-then-compute sequence can leave memory waits exposed.', 'Check whether pipelining or another buffer can overlap independent work.', 'Producer/consumer scheduling and target hardware are needed.'],
  'cuda-parallelism': ['Too little independent work can leave multiprocessors underused.', 'Compare grid size and work per block with the problem size.', 'Runtime dimensions, launch and GPU are needed.'],
  'cuda-tensor-cores': ['Scalar matrix arithmetic may leave a suitable matrix-instruction path unused.', 'Check datatype, tile alignment and numerical requirements.', 'Datatypes, matrix shapes and target GPU are needed.'],
  'triton-bounds': ['Tail lanes may access elements beyond the logical tensor.', 'Check masks on loads and stores for partial tiles.', 'Tensor dimensions and launch grid are needed.'],
  'triton-reduction': ['The wrong masked identity can change the reduction result.', 'Match masked values to the reduction, including negative inputs.', 'The reduction and valid input range are needed.'],
  'triton-layout': ['An unfavorable lane layout can add transactions or conversions.', 'Inspect contiguous dimensions and lane assignment for actual strides.', 'Input strides and kernel layout are needed.'],
  'triton-pressure': ['Large live tiles can increase register pressure and spills.', 'Compare smaller tiles and inspect compiled resources.', 'Tile sizes, warps and compiled resources are needed.'],
  'triton-pipeline': ['Serial load/compute scheduling can expose memory latency.', 'Check whether stages and independent work permit a pipeline.', 'The complete loop and target configuration are needed.'],
  'triton-dot': ['Expanded multiply/reduce operations may miss a suitable dot implementation.', 'Check whether tl.dot preserves the shape and numerical contract.', 'Operand types, shapes and layout are needed.'],
  'triton-grid': ['Grid mapping may leave output gaps or duplicate writes.', 'Check program IDs, tile extents and partial-tile coverage.', 'Launch grid and output dimensions are needed.'],
  'train-grad-reset': ['Missing or misplaced resets can accumulate unintended gradients or erase an update.', 'Check zero_grad relative to backward, step and accumulation boundaries.', 'Caller and gradient-reset/update boundaries are needed.'],
  'train-accumulation': ['Accumulation scaling can change the effective update magnitude.', 'Match loss reduction and scaling to the intended batch objective.', 'Loss reduction and the accumulation objective are needed.'],
  'train-device': ['Incompatible operand devices can stop training at runtime.', 'Check model, input, target and temporary tensor placement.', 'Model/input construction and caller device placement are needed.'],
  'train-mode': ['Evaluation behavior or disabled gradients can change the intended training computation.', 'Check training mode and gradient recording around the trainable forward.', 'Model mode and enclosing gradient context are needed.'],
  'train-amp': ['Clipping scaled gradients can distort the clipping threshold.', 'Check unscale before clipping, then scaler step and update.', 'Scaler, backward, clipping and update order are needed.'],
  'train-loader': ['Restarting the iterator can repeatedly train on the beginning of the dataset.', 'Check that one iterator advances through successive batches.', 'Loader construction and enclosing iteration are needed.'],
  'train-host-sync': ['Reading GPU values on the host can force a wait every update.', 'Check whether logging can aggregate on device and read back less often.', 'Tensor device and host-read frequency are needed.'],
  'train-retention': ['Retained graph-connected values can keep autograd memory alive.', 'Check logging containers and detach values that do not need gradients.', 'Container lifetime and retained tensors are needed.'],
  'train-step-order': ['An update before its backward uses missing or stale gradients.', 'Trace backward and step for the parameters being optimized.', 'The full update sequence and optimizer parameters are needed.'],
  'train-distributed': ['Mismatched collective participation can hang distributed training.', 'Check that required ranks execute matching synchronization.', 'Rank conditions and distributed setup are needed.'],
  'train-graph': ['A detached optimized loss cannot propagate gradients to its original parameters.', 'Trace the optimized loss to model output, separating teacher and logging branches.', 'Optimized loss, trainable parameters and caller are needed.'],
  'train-optimizer': ['Recreating the optimizer discards momentum or adaptive state from prior updates.', 'Keep optimizer initialization outside repeated updates to the same parameters.', 'Optimizer construction and the repeated update loop are needed.'],
  'train-loss': ['The wrong prediction representation can change the loss objective.', 'Check prediction and target arguments against the loss API contract.', 'Prediction representation and target shape/type are needed.'],
  'train-scheduler': ['A scheduler at the wrong boundary can shift the learning-rate schedule.', 'Check scheduler order and cadence against actual optimizer updates.', 'Scheduler type and intended cadence are needed.'],
  'train-accum-boundary': ['A reset or step at the wrong boundary breaks the intended microbatch window.', 'Check reset/step boundaries and any partial final group.', 'Accumulation window and final-group policy are needed.'],
  'train-validation': ['Validation settings can leak into later training and change its behavior.', 'Isolate validation and restore training behavior before updates resume.', 'Validation entry/exit and subsequent training are needed.'],
  'train-ddp-accum': ['Every-microbatch synchronization can add avoidable communication.', 'Check no_sync covers intermediate forwards and backwards, with a final synchronized backward.', 'DDP setup and accumulation schedule are needed.'],
  'infer-grad': ['Recording a prediction graph can add unnecessary memory and autograd work.', 'Check no_grad or inference_mode covers prediction.', 'Enclosing decorators, callers and gradient requirements are needed.'],
  'infer-mode': ['Training-mode dropout or normalization can change inference outputs.', 'Check eval and functional operations with explicit training flags.', 'Model setup, mode changes and dropout settings are needed.'],
  'infer-device': ['Model/input device mismatches can fail during prediction.', 'Check model, input and temporary tensor placement.', 'Serving setup and caller device placement are needed.'],
  'infer-transfers': ['Repeated host round trips can interrupt device execution.', 'Keep intermediates on device where possible and defer final readback.', 'Device contracts and loop boundaries are needed.'],
  'infer-cache': ['Copying growing state repeatedly increases traffic as the sequence grows.', 'Check indexed writes, bounded storage or reusable buffers.', 'State growth and output requirements are needed.'],
  'infer-attention': ['Materializing the attention matrix can add substantial intermediate traffic.', 'Check whether SDPA supports the same mask and numerical contract.', 'Attention shapes, masks and precision are needed.'],
  'infer-kv-cache': ['Recomputing the prefix repeats previous-token work on every decode step.', 'Check cache reuse and pass only new unprocessed tokens after prefill.', 'Prefill/decode loop and cache support are needed.'],
  'infer-causal-mask': ['Future-token access can invalidate a causal attention computation.', 'Check causal masking; distinguish prefill from newest-token cached queries.', 'Causal intent, query/key positions and masks are needed.'],
  'infer-padding': ['Unmasked padding can influence mixed-length batch outputs.', 'Forward the corresponding attention mask with padded inputs.', 'Padding setup and model mask arguments are needed.'],
  'infer-setup': ['Loading or constructing the model per request repeats setup work.', 'Check whether startup or service initialization can own the instance.', 'Service constructor, startup and handler lifetime are needed.'],
  'infer-retention': ['Unbounded request history can keep GPU storage alive.', 'Check persistent container bounds; detach does not release storage.', 'Container bounds and service lifetime are needed.'],
  'infer-attention-dropout': ['Functional attention dropout can stay active in eval mode.', 'Check zero inference dropout or the explicit training-dependent argument.', 'Functional dropout arguments and execution mode are needed.'],
  'tensor-transfer': ['Blocking CPU-to-GPU submissions can make the host wait each batch.', 'Check pinning and nonblocking submission; measure overlap separately.', 'Source device, pinning and transfer arguments are needed.'],
  'tensor-batching': ['Separate independent-item submissions can add Python and launch overhead.', 'Check a batched operation within the memory limit.', 'Iteration dependencies, shapes and memory limits are needed.'],
  'tensor-allocator': ['Repeated cache flushing prevents reuse of freed CUDA allocations.', 'Check whether empty_cache belongs outside the hot loop.', 'Release frequency and required memory handoffs are needed.'],
  'tensor-compile': ['Tensor-dependent Python can break a graph or fail in fullgraph mode.', 'Check tensor control such as where/cond and compile settings.', 'Compile wrapper, scalar-capture settings and control inputs are needed.'],
  'tensor-numerics': ['Unstabilized exponentials can overflow or underflow before normalization.', 'Check a stable primitive, max subtraction or an input-range guarantee.', 'Normalization expression and allowed range are needed.'],
  'tensor-layout': ['Incompatible views can fail; layout copies can add bandwidth cost.', 'Check strides before view and whether reshape or contiguous is needed.', 'Tensor shape, strides and dimension merge are needed.'],
  'data-iterator': ['Restarting an iterator can repeat early samples.', 'Check that iteration persists across batches.', 'Loader construction and iterator lifetime are needed.'],
  'data-transfer': ['Blocking transfers can expose waits in the input pipeline.', 'Check pinning, nonblocking submission and stream ordering.', 'Source storage, devices and stream ordering are needed.'],
  'data-preprocess': ['Serial CPU preprocessing can delay the next GPU batch.', 'Check worker preprocessing, precomputed transforms or prefetching.', 'Preprocessing cost, workers and GPU submission are needed.'],
  'general-numerics': ['Unstable arithmetic can produce invalid values for allowed inputs.', 'Check input range, precision and stable primitives.', 'The expression and allowed input range are needed.'],
  'general-lifetime': ['Unbounded retention can exhaust resources over time.', 'Check ownership, bounds and release after use.', 'Resource ownership and full lifetime are needed.'],
};
