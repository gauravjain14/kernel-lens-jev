{
  "version": "0.2.0",
  "configurations": [{
    "name": "Run Kernel Lens", "type": "extensionHost", "request": "launch",
    "args": ["--extensionDevelopmentPath=${workspaceFolder}", "${workspaceFolder}/examples"],
    "outFiles": ["${workspaceFolder}/dist/**/*.cjs"], "preLaunchTask": "npm: build"
  }]
}
